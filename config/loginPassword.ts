export type LoginPassword = {
    username: string;
    password: string;
}

/// Edit this to your own login password and username
const loginPassword: LoginPassword = {
    username: 'admin',
    password: 'admin'
}

export default loginPassword;